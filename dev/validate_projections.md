# Backtest findings: v1 vs v2 projection methods

Generated by `dev/validate_projections.R` (2026-07-03). Holdout seasons
2019–2025; for each holdout, outcomes/crosswalk trained only on prior seasons
(2012+) via a filtered `ffsimulator.cache_directory`; 200 simulated seasons per
player per version; weeks 1–14; QB/RB/WR/TE. Actuals = nflverse weekly scoring
(1QB-league scoring via mfl 47747 rules). Known mild leakage: `fp_injury_table`
is fit on all seasons.

Ideal values: cover50 = 0.50, cover80 = 0.80, PIT mean = 0.50, PIT tail share
(share of actuals below the sim 10th or above the 90th percentile) = 0.20.

## 1. Both versions are badly under-dispersed — the iid-weeks problem is real

Across positions and preseason-rank tiers, actual season totals land in the
tails of the simulated distributions 2–3x too often:

- cover50 ≈ 0.16–0.41 (should be 0.50), cover80 ≈ 0.30–0.75 (should be 0.80)
- PIT tail share ≈ 0.36–0.66 (should be 0.20)

Sampling weeks iid conditional on a season-level rank cannot produce enough
between-season spread: real seasons are autocorrelated (injury stints, role
changes, breakouts). This is the single biggest calibration problem and it
directly under-states the risk of "hyperfragile" rosters. → Phase 3 fix #3
(sample whole historical week-rank trajectories).

## 2. v2 as implemented is *worse* than v1, not better

For startable tiers (preseason rank 1–36), v1 beats v2 on every metric:

| metric (rank 1–12 tier) | v1 | v2 |
|---|---|---|
| QB cover50 / cover80 | 0.31 / 0.47 | 0.16 / 0.30 |
| QB MAE (season pts) | 95 | 130 |
| RB cover50 / cover80 | 0.35 / 0.51 | 0.21 / 0.46 |
| Spearman (all QB) | 0.80 | 0.78 |

v2's PIT mean for ranks 1–36 is 0.61–0.74 (should be 0.50): actual totals
consistently exceed the simulated distribution, i.e. **v2 systematically
under-projects startable players**. Two mechanisms:

1. The draft→week crosswalk regresses top draft ranks toward mediocre weekly
   ranks (a draft-rank-3 player's pool includes his bad weeks *and* worse
   players' weeks via `.ff_rank_expand`), and
2. the NA-padding in `ffs_adp_outcomes_week()` (`.ff_rep_na`) converts thin
   pools into extra zero-weeks; ~31% of v2 weekly draws are NA→0 in a typical
   sim.

Deep players (37+) show the opposite sign (PIT mean 0.23–0.42: over-projection)
in both versions. The theory (draft rank → weekly ranks → points) is not
invalidated — this *implementation* of it loses to the direct mapping.

## 3. Bye-week distortion: real, but concentrated in deep ranks

Actual points at the same weekly-rank band, by number of teams on bye
(0 byes → 5+ byes):

- Ranks 1–24: negligible (±1 point, no consistent direction).
- QB 25–36: **25.4 → 16.7** (−34%); RB 25–36: 11.0 → 9.5 (−14%);
  TE 25–36: 7.1 → 5.6 (−21%); WR 25–36: 11.5 → 11.0 (−5%).
- 37+ bands drop 5–20% as byes increase.

Interpretation: in heavy-bye weeks the rank-25+ pool is diluted with fill-in
players. Startable ranks are safe, but this matters exactly where
replacement-level is computed — which is what a WAR metric leans on.
→ Phase 3 fix #1 should condition on (or normalize away) bye load for ranks
25+, not globally.

## 4. Crosswalk survivorship: negligible for startable ranks

Preseason-ranked player-seasons that never received a weekly ranking: ≤1.2%
for ranks 1–36 across all positions, but 15% (RB/WR/TE) to 37% (QB) of 37+
players. With the new sample-crash guards these silently drop from v2 sims.
Fine for starters, biased for deep-roster/replacement questions. → Phase 3
fix #2 (retain as explicit zero/unranked outcomes) matters for WAR, not for
top-of-roster projections.

## 5. v3 (whole-trajectory resampling) largely fixes the dispersion problem

Update 2026-07-03: implemented `version = "v3"` — per simulated season, sample
one whole historical player-season of weekly ranks (matched on pos + draft
rank ±2, byes inferred from the weekly rankings themselves), then map each
weekly rank to a score draw. Backtest (same protocol, all three versions):

| metric (startable tiers, ranks 1–36) | v1 | v2 | v3 |
|---|---|---|---|
| cover50 (ideal 0.50) | 0.23–0.39 | 0.12–0.30 | **0.32–0.49** |
| cover80 (ideal 0.80) | 0.40–0.61 | 0.32–0.56 | **0.45–0.73** |
| PIT tail share (ideal 0.20) | 0.40–0.60 | 0.46–0.66 | **0.19–0.34** |
| PIT mean (ideal 0.50) | 0.48–0.57 | 0.61–0.74 | **0.46–0.60** |
| Spearman (all players) | 0.77–0.80 | 0.76–0.78 | 0.77–0.79 |

Deep tiers (37+): v3 cover80 = 0.84–0.93, near ideal; v1/v2 = 0.42–0.75.
MAE is within a few points of v1 everywhere — the calibration gain is free.
v3 also drops zero ranked players (trajectory pools retain player-seasons
with no weekly rankings as all-NA = zero-point seasons), structurally fixing
the survivorship issue (#4).

Residual: elite QB (tier 1–12) is still under-dispersed (tail share 0.53) —
top-rank trajectory pools are thin; consider widening `.ff_rank_expand` size
for ranks ≤ 12 or pooling adjacent seasons.

**Bye adjustment (v3 only):** weekly ranks are rescaled to full-slate
equivalents (`.ff_bye_adjust_rank()`: rank × n_teams / n_teams_playing, byes
inferred from rankings coverage) in both the outcome pools and the trajectory
values. Re-running the backtest showed this is *neutral on season-total
calibration* (mean coverage deltas < 0.01, MAE −0.1) — the per-week
distortion averages out over 14 weeks. It is kept because it makes the
weekly rank → points mapping semantically correct at the week level, which
is where WAR replacement-level comparisons happen, and it costs nothing.

**Recommendation: v3 should become the default for season simulation and the
basis for WAR.** It directly implements the "preseason rank → weekly ranks →
points" theory with coherent seasons instead of iid weeks.

## Recommendations for Phase 3 (priority order, revised)

1. **Within-season autocorrelation** (was #3) — biggest calibration win for
   both versions; prerequisite for trustworthy dynasty-fragility outputs.
2. **v2 top-rank under-projection** — revisit `.ff_rank_expand` on the
   crosswalk and the NA-padding semantics before treating v2 as the default;
   as shipped, v2 should not be preferred over v1.
3. **Bye-conditioning for ranks 25+** (was #1) — needed for credible
   replacement level in WAR; skip global normalization.
4. **Crosswalk dropout retention** (was #2) — same motivation as #3.

Raw outputs: `dev/validate_outputs/*.csv` (per-player results, summary
metrics, spearman, dropout, bye effect, survivorship).
