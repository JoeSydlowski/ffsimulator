% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/12-fantasycalc.R
\name{fc_dynasty_values}
\alias{fc_dynasty_values}
\title{Scrape current FantasyCalc dynasty (or redraft) values}
\usage{
fc_dynasty_values(num_qbs = 2, num_teams = 12, ppr = 1, is_dynasty = TRUE)
}
\arguments{
\item{num_qbs}{starting QBs: 1 (1qb) or 2 (superflex); sets the \code{format} label}

\item{num_teams}{league size (default 12)}

\item{ppr}{points per reception (default 1)}

\item{is_dynasty}{TRUE for dynasty values, FALSE for redraft (default TRUE)}
}
\value{
a dataframe: \code{scraped_date}, \code{format}, league settings, \code{fantasypros_id},
player fields, and values (\code{value}, \code{overall_rank}, \code{pos_rank}, \code{trend_30day},
\code{redraft_value}, \code{value_sd}, \code{tier}, \code{adp}) plus the source ids
}
\description{
(EXPERIMENTAL) Pulls the live player-value table from the FantasyCalc API for
a given league configuration and crosswalks each player to a \code{fantasypros_id}
(via \code{ffscrapr::dp_playerids()}, matching on Sleeper then MFL id) so the
values join straight onto the rest of the ffsimulator machinery.
}
\details{
FantasyCalc publishes market values derived from real dynasty trades; these
are a far better anchor than a synthetic rank-decay curve. \code{num_qbs} controls
the QB format (1 = 1QB, 2 = superflex), which materially changes QB values.
}
