% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/13-picks.R
\name{ffs_pick_values}
\alias{ffs_pick_values}
\title{Value future draft picks as dynasty assets}
\usage{
ffs_pick_values(
  base_simulation,
  picks = NULL,
  conn = NULL,
  pick_curve = "dev/data/pick_value_curve.csv",
  fc_pick_values = NULL,
  format = "auto",
  sim_season = NULL
)
}
\arguments{
\item{base_simulation}{an \code{ff_simulation} (\code{return = "all"})}

\item{picks}{a \code{ffs_draftpicks()} dataframe; if \code{NULL}, fetched via \code{conn}}

\item{conn}{a league connection (used only when \code{picks} is \code{NULL})}

\item{pick_curve}{the empirical curve: a data.frame or path to
\code{pick_value_curve.csv}}

\item{fc_pick_values}{a pre-scraped \code{fc_dynasty_values()} df (pick rows); if
\code{NULL}, scraped for the simulation's format}

\item{format}{\code{"auto"}, \code{"1qb"}, or \code{"superflex"} (auto-detected from the
lineup constraints, as in \code{ffs_dynasty_outlook})}

\item{sim_season}{the season being simulated (default: one before the earliest
pick season)}
}
\value{
a data.frame, one row per pick, with \code{ffs_dynasty_outlook}-compatible
columns plus \code{season}, \code{round}, \code{exp_pick} (expected overall pick number),
\code{emp_ev}, and \code{original_franchise_id}
}
\description{
(EXPERIMENTAL) Prices each future rookie pick a league's franchises hold, so
picks can sit in the same dynasty/trade machinery as players. A pick is a
zero-wins-now, store-of-value asset: its worth is set by the \strong{slot} it will
convert at (inverse of the origin team's projected finish) and the historical
hit rate at that slot.
}
\details{
The value model (Joe's "option 1"): \code{cur_value} is the \strong{market} price of the
slot (FantasyCalc's 12-team pick curve, evaluated at the projected overall
pick number and discounted by the market's year-over-year decay). \code{next_value}
is the same slot one year closer to converting, so picks \strong{appreciate} toward
their draft (a pure store of value, \code{win_now_value <= 0}). The empirical
hit-rate curve (\code{dev/data/pick_value_curve.csv}, from \code{pick_value_study.R})
supplies the median/tail skew and an \code{emp_ev} column for the market-vs-hit-rate
edge.

Column contract matches \code{\link[=ffs_dynasty_outlook]{ffs_dynasty_outlook()}} so downstream code
(\code{ffs_build_trades}, the trade suite) treats picks and players uniformly.
}
