% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/14-keepers.R
\name{ffs_keeper_world}
\alias{ffs_keeper_world}
\title{Score one keeper/draft world}
\usage{
ffs_keeper_world(
  rosters,
  projected_scores,
  franchises,
  lineup_constraints,
  latest_rankings,
  schedules,
  playoff_slots = 6L,
  playoff_weeks = NULL,
  weeks_per_round = 1L,
  pos_filter = c("QB", "RB", "WR", "TE"),
  replacement_level = TRUE,
  lineup_method = "rank"
)
}
\arguments{
\item{rosters}{hypothetical rosters - needs \code{league_id}, \code{franchise_id},
\code{franchise_name}, \code{player_id}, \code{fantasypros_id}, \code{pos}}

\item{projected_scores}{projections from \code{\link[=ffs_generate_projections]{ffs_generate_projections()}}}

\item{franchises}{a franchises dataframe (\code{\link[=ffs_franchises]{ffs_franchises()}})}

\item{lineup_constraints}{from \code{\link[=ffs_starter_positions]{ffs_starter_positions()}}}

\item{latest_rankings}{from \code{\link[=ffs_latest_rankings]{ffs_latest_rankings()}}, used for replacement level}

\item{schedules}{from \code{\link[=ffs_build_schedules]{ffs_build_schedules()}} - its weeks define the regular season}

\item{playoff_slots}{number of playoff berths}

\item{playoff_weeks}{optional weeks (present in \code{projected_scores} but not in}

\item{weeks_per_round}{games per bracket round (2 for a league whose
playoff rounds run two weeks and advance on combined score)
\code{schedules}) used to play out a single-elimination bracket for
\code{champion_pct}; \code{NULL} skips it}

\item{pos_filter}{positions to simulate}

\item{replacement_level}{add waiver-wire replacement players to every roster}

\item{lineup_method}{passed to \code{\link[=ffs_optimise_lineups]{ffs_optimise_lineups()}}}
}
\value{
a data.table with one row per franchise: \code{h2h_wins}, \code{points_for},
\code{playoff_pct}, and \code{champion_pct} when \code{playoff_weeks} is supplied
}
\description{
(EXPERIMENTAL) Runs a set of hypothetical rosters through the back half of
the simulation pipeline and returns each franchise's standings summary.
\code{projected_scores} and \code{schedules} are deliberately inputs rather than being
regenerated: comparing two keeper sets is a paired comparison, so every world
must share the same player-score draws and the same schedule, and differ only
in who is on which roster.
}
\details{
Playoff seeding is wins, then points-for, resolved deterministically - the
same convention as \code{\link{.ffs_summarise_optimal}}. Random tie-breaks re-roll
between worlds and leak into the delta.
}
