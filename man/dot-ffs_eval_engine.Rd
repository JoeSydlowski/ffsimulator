% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/14-eval-engine.R
\name{.ffs_eval_engine}
\alias{.ffs_eval_engine}
\title{Incremental trade-evaluation engine}
\usage{
.ffs_eval_engine(base_simulation)
}
\arguments{
\item{base_simulation}{an \code{ff_simulation} from \code{return = "all"}}
}
\value{
an \code{ffs_eval_engine} object, or \code{NULL} when the simulation is not
eligible (non-\code{rank} lineup method, best-ball, or a ragged franchise-week
panel) - callers fall back to the legacy path.
}
\description{
\code{\link[=ffs_trade_eval]{ffs_trade_eval()}} re-summarises the whole league twice per call - once for
the unchanged \code{before} state and once for \code{after} - and each summary copies
the full \code{optimal_scores} table (list-columns included), re-ranks every
season-week for all-play, and re-joins the schedule. On a 2000-season
simulation that is ~19 seconds a deal, which caps a confirmation run at a few
dozen trades.
}
\details{
Almost all of that work is invariant. A trade changes exactly two franchises'
weekly scores; the schedule, the season-week grid and the other franchises'
scores never move. This engine pre-computes the invariant structure once and
represents the league as a \verb{[season-week x franchise]} score matrix, so a deal
costs two column overwrites plus a handful of vectorised matrix reductions.
The \code{before} summary is computed once at build time and reused.

The engine reproduces \code{\link[=.ffs_summarise_optimal]{.ffs_summarise_optimal()}} exactly, including its
rounding quirks: \code{result} is decided on \strong{unrounded} scores while
\code{points_for} sums scores \strong{rounded to 2dp}, all-play wins use average tie
ranks, and the playoff seeding breaks ties by franchise_id order. See
\code{tests/testthat/test-trade-engine.R} for the equivalence suite.
}
\keyword{internal}
