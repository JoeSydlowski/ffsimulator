% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/14-keepers.R
\name{ffs_keeper_costs}
\alias{ffs_keeper_costs}
\title{Keeper cost for every rostered player}
\usage{
ffs_keeper_costs(
  rosters,
  prev_picks,
  adp_ranks = NULL,
  n_teams = 12L,
  escalator = 1L,
  floor_round = 1L,
  max_round = NULL
)
}
\arguments{
\item{rosters}{a rosters dataframe (\code{ffs_rosters()}) - needs \code{player_id},
\code{franchise_id}, \code{franchise_name}, \code{pos}, \code{fantasypros_id}}

\item{prev_picks}{the previous season's draft board - needs \code{player_id} and
\code{round}, matching \code{rosters$player_id} (i.e. platform ids)}

\item{adp_ranks}{optional dataframe of \code{fantasypros_id} + \code{adp_rank} (an
overall 1..N ordering) used to price players with no prior draft round}

\item{n_teams}{number of franchises, used to convert an ADP rank to a round}

\item{escalator}{how many rounds a keeper's cost escalates per year (1 = the
usual "round drafted minus one")}

\item{floor_round}{the earliest round a keeper can cost (1 = a first-round
keeper stays a first-round keeper)}

\item{max_round}{the last round of the draft; ADP-priced players beyond it
get \code{base_round = max_round}. \code{NULL} leaves them uncapped.}
}
\value{
the rosters dataframe with \code{prev_round}, \code{base_round},
\code{top2_prev} (drafted in the previous draft's first two rounds - the input
to a "keep only one of your top two rounds" rule) and \code{cost_source}
(\code{"draft"}, \code{"adp"} or \code{NA} when the player cannot be priced)
}
\description{
(EXPERIMENTAL) Resolves the draft round each rostered player would cost to
keep. The common keeper convention is "the round you drafted him, minus one,
floored at round 1"; players acquired off waivers have no prior draft round
and are instead priced at their current ADP round.
}
