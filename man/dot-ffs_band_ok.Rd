% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/15-deal-scores.R
\name{.ffs_band_ok}
\alias{.ffs_band_ok}
\title{Vectorised value-band test for a set of candidate send/receive pairs}
\usage{
.ffs_band_ok(
  send_value,
  recv_value,
  n_send,
  n_recv,
  fair_premium = 0.05,
  fair_band = NULL,
  even_band = 0.15,
  uneven_gap = NULL,
  consolidation_penalty = 0,
  uneven_shade = even_band
)
}
\arguments{
\item{send_value, recv_value}{vectors of summed value each way}

\item{n_send, n_recv}{the shape (scalars - one shape per call)}

\item{fair_premium, fair_band}{see \code{\link[=ffs_build_trades]{ffs_build_trades()}}}

\item{even_band, uneven_gap, consolidation_penalty, uneven_shade}{legacy bands}
}
\value{
a logical vector, \code{TRUE} where the pair is inside the band
}
\description{
\code{fair_band} (preferred) tests the deal's edge against the shape-fair price in
one symmetric band. Falling back, the legacy per-shape bands apply: a
symmetric \code{even_band} on same-count trades, and an overpay premium band on
uneven ones (the multi-player side must overpay the single player).
}
\keyword{internal}
