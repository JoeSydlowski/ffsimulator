% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/10-trade.R
\name{.ffs_counterfactual_rows}
\alias{.ffs_counterfactual_rows}
\title{Exact counterfactual franchise optimal-scores rows for a set of add/remove moves}
\usage{
.ffs_counterfactual_rows(
  base_simulation,
  fid,
  remove_ids = character(0),
  add_ids = character(0)
)
}
\arguments{
\item{base_simulation}{an \code{ff_simulation} from \code{return = "all"}}

\item{fid}{the franchise whose roster changes}

\item{remove_ids}{player_ids leaving \code{fid} (can be empty)}

\item{add_ids}{player_ids joining \code{fid} (can be empty)}
}
\value{
\code{fid}'s optimal_scores rows with \code{actual_score} updated in the affected
weeks (other columns carry base values)
}
\description{
The base simulation's lineups already stand for every franchise-week. Removing
players only changes weeks where one of them was actually started; adding
players only changes weeks where one would crack the lineup (clears the started
cut-line, or the base lineup had an unfilled slot that week, e.g. a bye).
Removing a started player frees a slot an added player may fill, but such weeks
are already flagged by the removal. This re-optimises only the affected weeks
with \code{\link[=.ffs_optimise_started]{.ffs_optimise_started()}} (no hindsight LP), reuses the base
\code{actual_score} everywhere else, and returns the franchise's modified
optimal_scores rows. Identical to a full re-optimisation; only faster. Assumes
\code{lineup_method = "rank"} (guarded by callers). Shared by \code{\link[=ffs_player_value]{ffs_player_value()}}
(one add or one remove) and \code{\link[=ffs_trade_eval]{ffs_trade_eval()}} (a package each way).
}
\keyword{internal}
