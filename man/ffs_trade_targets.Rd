% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/10-trade.R
\name{ffs_trade_targets}
\alias{ffs_trade_targets}
\title{Scan the league for trade fits}
\usage{
ffs_trade_targets(base_simulation, franchise_id, top_n = 20)
}
\arguments{
\item{base_simulation}{an \code{ff_simulation} object from \code{ff_simulate(..., return = "all")}}

\item{franchise_id}{the acquiring franchise}

\item{top_n}{how many screened candidates to value exactly (default 20)}
}
\value{
a dataframe of candidates: screen proxy, value_to_you +
playoff_delta_you (impact on YOUR team), value_to_owner +
playoff_delta_owner (impact on the OWNER's team), and
surplus = value_to_you - value_to_owner
}
\description{
(EXPERIMENTAL) Two-stage scan for a franchise: a cheap screen ranks every
player rostered elsewhere by how many expected points he would add to this
franchise's lineup (no optimization), then the shortlist is valued exactly
with \code{ffs_player_value()} for both this franchise and the player's current
owner. The interesting column is \code{surplus} - players worth more to you
than to the team that owns them are where trades get made.
}
