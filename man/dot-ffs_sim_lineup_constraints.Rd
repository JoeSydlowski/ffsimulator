% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/10-trade.R
\name{.ffs_sim_lineup_constraints}
\alias{.ffs_sim_lineup_constraints}
\title{Lineup constraints for the positions a simulation actually simulated}
\usage{
.ffs_sim_lineup_constraints(
  base_simulation,
  lineup_constraints = NULL,
  roster_scores = NULL
)
}
\arguments{
\item{base_simulation}{an \code{ff_simulation} from \code{return = "all"}}

\item{lineup_constraints}{optional pre-converted constraints (defaults to the
simulation's own); filtering an already-filtered table is a no-op}

\item{roster_scores}{optional pre-converted roster scores, used only to
recover the simulated positions when \code{simulation_params} carries no
\code{pos_filter} (older objects)}
}
\value{
\code{lineup_constraints} as a data.table, restricted to simulated positions
}
\description{
\code{\link[=ffs_optimise_lineups]{ffs_optimise_lineups()}} filters \code{lineup_constraints} to \code{pos_filter} before
building its LP, so a position that is never simulable (DEF has no rows in
the scoring history) is not part of the base lineups. Any path that
re-optimises against the base simulation has to filter identically: an
unfiltered \code{DEF} row carries \code{min = 1}, which the LP can only satisfy with a
zero-point filler, and that filler counts against \code{offense_starters} - so
every re-optimised week starts one fewer real player than its base week and
the resulting delta is wrong. Only bites when the league has a non-offensive
slot, i.e. \code{offense_starters != total_starters}.
}
\keyword{internal}
