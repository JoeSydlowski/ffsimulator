% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/14-keepers.R
\name{ffs_mock_draft}
\alias{ffs_mock_draft}
\title{Mock-draft the remaining pool}
\usage{
ffs_mock_draft(
  pool,
  pick_map,
  kept = NULL,
  pos_need = c(QB = 1L, RB = 2L, WR = 3L, TE = 1L),
  pos_cap = c(QB = Inf, RB = Inf, WR = Inf, TE = Inf),
  pos_start_max = NULL,
  over_start_decay = 0.15,
  rookie_boost = 0,
  soft_bench = 1L,
  window_k = 6L,
  rank_decay = 0.7,
  need_boost = 1.6,
  depth_decay = 0.4,
  seed = NULL
)
}
\arguments{
\item{pool}{available players - needs \code{fantasypros_id}, \code{player}, \code{pos},
\code{ecr} (a TRUE rank - board order is deterministic; the window +
roster-fit weighting below is what makes the pick itself stochastic)}

\item{pick_map}{picks to fill, as produced by \code{\link[=ffs_draft_pick_map]{ffs_draft_pick_map()}}}

\item{kept}{optional dataframe of players already on rosters (keepers) -
needs \code{franchise_id} and \code{pos} - used to seed each franchise's positional
counts}

\item{pos_need}{named integer vector of positions a franchise must end up
with (the hard starter floor - e.g. lineup minimums, not roster targets)}

\item{pos_cap}{named vector of positional roster maximums (\code{Inf} = no cap;
this is a hard ceiling like a platform's per-position roster limit, not a
preference - use \code{soft_bench}/\code{depth_decay} to softly discourage excess)}

\item{pos_start_max}{named vector of how many at each position can ever
START (lineup_constraints$max). Bodies beyond this are pure bench: a 4th QB
in a 2-QB-max lineup cannot play, so ranking him on board value alone makes
the drafter take players it can never use. \code{NULL} disables the penalty.}

\item{over_start_decay}{weight multiplier per body past \code{pos_start_max}}

\item{rookie_boost}{ranks to move rookies UP the board. Redraft ranking
services systematically distrust rookies, but drafters do not: in Joe's
league rookies went ~11.7 picks (~26 ranks) earlier than their preseason
rank implied, controlling for rank (p=0.03, 2024-25). Requires a logical
\code{rookie} column on \code{pool}; 0 disables.}

\item{soft_bench}{extra copies beyond \code{pos_need} a franchise wants before a
position starts getting discounted (bench/flex depth)}

\item{window_k}{how many of the best remaining eligible players (by true
rank) are in play for a given pick}

\item{rank_decay}{within the window, how much the pick still favors the top:
weight \code{exp(-rank_decay * (rank_in_window - 1))} before the fit multiplier}

\item{need_boost}{weight multiplier for a position below \code{pos_need}}

\item{depth_decay}{weight multiplier per pick beyond \code{pos_need + soft_bench}
at a position (\code{depth_decay^k} for the k-th excess copy)}

\item{seed}{optional integer seed}
}
\value{
a data.table of drafted players: \code{franchise_id}, \code{round}, \code{overall},
\code{fantasypros_id}, \code{player}, \code{pos}, \code{ecr}
}
\description{
(EXPERIMENTAL) Fills each franchise's remaining picks from the undrafted
pool. Each pick is drawn from a bounded window of the best remaining players
by true rank (not the full pool - a fringe player's rank uncertainty can
never launch him to the top of the board), weighted toward the top of that
window but tilted by roster construction: a franchise below its starter
floor at a position is boosted, one comfortably past it is discounted. A
hard backstop still forces a franchise to fill unmet starter needs once it
has only that many picks left, with no randomness - the guarantee that
every team finishes with a legal lineup should never depend on a coin flip.
}
