% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/14-eval-engine.R
\name{ffs_trade_eval_many}
\alias{ffs_trade_eval_many}
\title{Evaluate many trades against one simulation}
\usage{
ffs_trade_eval_many(
  base_simulation,
  deals,
  engine = NULL,
  progress = NULL,
  workers = 1L
)
}
\arguments{
\item{base_simulation}{an \code{ff_simulation} from \code{return = "all"}}

\item{deals}{a data.frame with columns \code{franchise_a}, \code{gives_a}, \code{franchise_b}
and \code{gives_b}; the two \code{gives_} columns are list-columns of player_ids}

\item{engine}{an existing engine from \code{\link[=ffs_trade_engine]{ffs_trade_engine()}}, or \code{NULL} to build one}

\item{progress}{optional function called as \code{progress(i, n)} after each deal
(sequential runs only)}

\item{workers}{number of parallel workers. \code{1} (default) runs in-process;
\code{NULL} picks \code{parallel::detectCores(logical = FALSE) - 2}. Parallel runs need
roughly 0.8 GB per worker on a 2000-season simulation.}
}
\value{
a data.frame with one row per deal per franchise (two rows a deal),
carrying a \code{deal_id} column plus the columns \code{\link[=ffs_trade_eval]{ffs_trade_eval()}} returns
}
\description{
Batch companion to \code{\link[=ffs_trade_eval]{ffs_trade_eval()}}. Builds (or reuses) an
\code{\link[=ffs_trade_engine]{ffs_trade_engine()}} so the invariant league summary is computed once for the
whole batch, then evaluates each deal.
}
\details{
Per-deal cost is dominated by re-solving both franchises' weekly lineups (one
small LP per franchise-week - ~56,000 of them a deal on a 2000-season
simulation), which is irreducible if results are to stay bit-identical. The
deals are independent, though, so \code{workers > 1} spreads them over a PSOCK
cluster. Each worker loads a slimmed copy of the simulation from a temporary
file rather than having it serialised down the socket.
}
