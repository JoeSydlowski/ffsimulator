% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/10-trade.R
\name{.ffs_value_counterfactual}
\alias{.ffs_value_counterfactual}
\title{Exact counterfactual franchise summary for adding/removing one player}
\usage{
.ffs_value_counterfactual(base_simulation, pid, fid, owned)
}
\arguments{
\item{base_simulation}{an \code{ff_simulation} from \code{return = "all"}}

\item{pid}{the player_id being added/removed}

\item{fid}{the franchise the counterfactual is for}

\item{owned}{\code{TRUE} if \code{pid} is currently on \code{fid} (removal), else addition}
}
\description{
The base simulation's lineups already stand for every franchise-week. Removing
a player only changes the weeks he was actually started; adding a player only
changes the weeks he would crack the lineup (he clears the started cut-line, or
the base lineup had an unfilled slot that week, e.g. a bye). This re-optimises only those weeks -
with \code{\link[=.ffs_optimise_started]{.ffs_optimise_started()}} (no hindsight LP) - reuses the base
\code{actual_score} everywhere else, and returns the modified franchise summary.
Identical to a full re-optimisation; only faster. Assumes \code{lineup_method = "rank"} (guarded by the caller).
}
\keyword{internal}
