% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04-projections.R
\name{.ffs_draft_to_week_trajectories}
\alias{.ffs_draft_to_week_trajectories}
\title{Build draft-rank keyed pools of historical weekly-rank trajectories}
\usage{
.ffs_draft_to_week_trajectories(
  max_week = 16,
  bandwidth = getOption("ffsimulator.v3_bandwidth", c(QB = 11, RB = 7, WR = 7, TE = 4)),
  bandwidth_slope = getOption("ffsimulator.v3_bandwidth_slope", NULL),
  bandwidth_floor = getOption("ffsimulator.v3_bandwidth_floor", 2),
  bandwidth_cap = getOption("ffsimulator.v3_bandwidth_cap", 15)
)
}
\description{
For every preseason-ranked player-season, records the full sequence of
weekly FantasyPros ranks over that season's non-bye weeks (NA where the
player went unranked). Pools these trajectories by position and draft rank,
so a simulated season can resample one coherent historical season rather
than independent weeks. Player-seasons with zero weekly rankings are
retained as all-NA trajectories, pricing in flameout/irrelevance risk.
}
\details{
Pool width around each draft rank is controlled by
\code{options(ffsimulator.v3_bandwidth = c(QB = 4, RB = 6, ...))}: a triangular
kernel replicates each trajectory into nearby draft ranks with weight
declining in rank distance (bandwidth = the distance at which weight hits
zero). Positions not named in the option fall back to bandwidth 2. The
defaults were tuned by held-out backtest (dev/validate_projections.md) -
QB needs wide pools (elite QB seasons are scarce), TE narrow ones (steep
quality cliff). Set the option to \code{NA} to use the legacy hard +/-2 uniform
window (\code{.ff_rank_expand()}).

Set \code{options(ffsimulator.v3_bandwidth_slope = c(QB = 0.35, ...))} to make the
bandwidth grow with rank instead of being constant: \code{h(pos, rank) = clamp(slope * rank, floor, cap)} with \code{ffsimulator.v3_bandwidth_floor}
(default 2) and \code{ffsimulator.v3_bandwidth_cap} (default 15). This gives
narrow pools at the top of a position (where the value-over-rank curve is
steep, so borrowing biases) and wide pools at the thin, flat bottom (where
borrowing only cuts variance). \code{h} is a property of the target rank a pool
serves. When \code{bandwidth_slope} is unset the bandwidth stays constant per
position (the tuned defaults above).
}
\keyword{internal}
