% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/11-dynasty.R
\name{.ffs_draw_transition}
\alias{.ffs_draw_transition}
\title{Draw dynasty transitions by kernel-weighted resampling}
\usage{
.ffs_draw_transition(
  pos,
  age,
  dyn_pos_rank,
  q,
  transitions,
  h_rank = 8,
  h_age = 3,
  h_q = 0.25,
  years_exp = NULL,
  draft_round = NULL,
  prev_delta = NULL,
  ecr_sd = NULL,
  h_exp = getOption("ffsimulator.dyn_h_exp", NA),
  h_draft = getOption("ffsimulator.dyn_h_draft", NA),
  h_momentum = getOption("ffsimulator.dyn_h_momentum", NA),
  h_sd = getOption("ffsimulator.dyn_h_sd", NA),
  rookie_strict = getOption("ffsimulator.dyn_rookie_strict", FALSE),
  exit_shrink = getOption("ffsimulator.dyn_exit_shrink", 10),
  exit_broad_rank = getOption("ffsimulator.dyn_exit_broad_rank", 3),
  exit_broad_age = getOption("ffsimulator.dyn_exit_broad_age", 3),
  exit_transitions = NULL,
  disp_factor = NULL,
  move_slope = NULL,
  move_bias = NULL,
  move_space = getOption("ffsimulator.dyn_move_space", "log")
)
}
\value{
a list of two parallel vectors: next_pos_rank, exited
}
\description{
For each target (pos, age, dynasty \emph{positional} rank, Q), samples one
historical transition weighted by positional-rank distance (triangular,
bandwidth 8), age distance (triangular, bandwidth 3), and Q distance
(triangular, bandwidth 0.25; targets or candidates without Q use a
neutral mid weight). The sampled \emph{positional-rank delta} is applied to
the target's own positional rank; sampled exits stay exits.
}
\details{
Optional kernel terms (all off by default; enabled via \verb{ffsimulator.dyn_*}
options or the bandwidth arguments, and gated on the multi-year dynasty
backtest before becoming defaults):
\itemize{
\item \code{years_exp} with \code{h_exp} - seasons since draft (rookie repricing differs
from same-age veterans); \code{dyn_rookie_strict} additionally restricts
rookie targets (years_exp 0) to rookie candidates when the pool allows
\item \code{draft_round} with \code{h_draft} - draft capital (UDFA = round 8)
\item \code{prev_delta} with \code{h_momentum} - last year's positional-rank move
\item \code{ecr_sd} with \code{h_sd} - expert disagreement (matches uncertain players to
historically uncertain candidates, whose transitions were wider)
Candidates missing an enabled feature get the neutral weight 0.3, matching
the age/Q convention; targets missing it skip the term.
}

Exit shrinkage (\code{exit_shrink} / \code{ffsimulator.dyn_exit_shrink}, default
kappa = 10): the raw exit probability is the kernel-weighted mean of the exit
indicator over the candidate pool - a Nadaraya-Watson estimate that is
high-variance for thin cells (e.g. mid-20s QBs at a fringe rank, whose
effective pool is ~5 historical players once age x rank x Q are all matched).
The local estimate is shrunk toward a broad (wide rank+age bandwidth)
empirical rate by effective sample size,
\code{p = (n_eff * local + kappa * broad) / (n_eff + kappa)}, and exit is then a
Bernoulli draw with the rank move resampled among survivors. This tempers
noisy per-cell exit spikes without flattening the genuine age/rank trend.
Set \code{kappa = 0} to recover the un-shrunk single-draw behavior. Enabled by
default after the multi-year dynasty backtest: at kappa = 10 the pooled exit
calibration improves (e.g. the 1qb >50\% bin, badly over-dispersed at
0.73 predicted / 0.52 actual, moves to 0.64 / 0.60) with survivor rank
calibration held flat; gains plateau by kappa 10-15.

\code{exit_transitions} (optional): a separate pool from which the EXIT
probability is estimated, while the rank move is still resampled from
\code{transitions}. Used by superflex, which borrows the deep 1qb pool for
movement dispersion but keeps its native (format-specific, ranking-depth)
exit rate - importing 1qb's higher skill-position exit rates otherwise
over-predicts superflex exits. \code{NULL} (default) uses \code{transitions} for both,
so 1qb leagues and every other caller are unchanged.

\code{move_slope} / \code{move_bias} (optional named per-position vectors, e.g.
\code{c(QB = 0.35, RB = 0.5)} / \code{c(RB = -1)}): point-prediction recalibration.
The multi-holdout backtest finds the regression slope of actual on
predicted rank move < 1 at every position (predicted move magnitudes too
extreme - regression to the mean not fully captured by the kernel
resample). The correction shifts each draw's CENTER: the kernel-weighted
mean move \code{dbar} becomes \code{move_slope * dbar + move_bias} (in the active
\code{move_space}'s units, positive = decline) while the dispersion around it is
left to \code{disp_factor}. The defaults are COUPLED to \code{move_space} (see there);
both sets are fit on material players (value >= 150) across all 11 holdouts,
leave-one-holdout-out stable. The shipped LOG set is
\code{move_slope = c(QB = .639, RB = .852, TE = .734, WR = .860)},
\code{move_bias = c(QB = .002, RB = -.027, TE = .055, WR = .016)} - the biases are
~0 because the log coordinate removes the systematic level shift the rank set
had to patch. The RANK set, used when \code{move_space = "rank"}, is
\code{move_slope = c(QB = .48, RB = .76, WR = .64, TE = .51)},
\code{move_bias = c(QB = 1.7, RB = 2.4, WR = 4.1, TE = 2.5)}; it lifts the material
actual~predicted slope toward 1 and cuts exp_change optimism, but its +4.1 WR
bias SATURATES at the apex (over-corrects elite ranks, cover80 0.41) - the
reason log became the default. See
dev/validate_outputs/dynasty_point_calibration.txt (sections 3 and 7). Set the
option to \code{numeric(0)} to disable; positions absent from the vector get
slope 1 / bias 0.

\code{disp_factor} (optional named per-position vector, e.g.
\code{c(QB = 1.7, TE = 1.4, WR = 1.2, RB = 1.1)}): widens each survivor's rank
move around its weighted-mean move by the position's factor, correcting the
interval under-dispersion of positions with compressed positional scales
(QB/TE rankings are shallow but map through a steep value curve, so the
conditional resample under-states their value volatility; effective sample
size is ~flat across positions, so this is a per-position, not n_eff-driven,
correction). The mean is preserved, so medians/\code{p_rise} are unchanged - only
the interval widens. Defaults are COUPLED to \code{move_space}: the shipped LOG set
is \code{c(QB = 1.20, TE = 1.13, WR = 1.06, RB = 1.03)} - the log coordinate is
already near-homoscedastic, so it needs less widening than the RANK set
(\code{c(QB = 1.68, TE = 1.43, WR = 1.20, RB = 1.11)}, used when
\code{move_space = "rank"}). Both lift survivor \code{cover80} toward ~0.80. Set the
option to \code{numeric(0)}, or pass a vector with no matching positions, to disable.

\code{move_space} (\code{"log"} default as of 2026-07-25, or \code{"rank"} / \code{"probit"}): the
coordinate the survivor rank move is recentered/widened and transported in.
\code{"rank"} is the original behavior - byte-identical RNG stream and results (the
additive positional-rank delta clipped at rank 1). The latent options instead apply
the move in a monotone, unbounded, RANK-ONLY transform \code{T} (\code{"log"}:
\verb{T(r)=log r}; \code{"probit"}: \code{T(r)=qnorm(r/(pool_depth+1))}), so the rank-1
reflecting boundary softens and the move is homoscedastic across the board:
\verb{next = Tinv(T(target) + [ms*Tbar + mb] + fp*(T(pick_move) - Tbar))}. This
targets the apex-truncation artifact the pass-2 rank-space correction
saturates against (elite ranks can't rise above 1, so their move center
stops responding to \code{move_slope}); moving in \code{T} lets the shrinkage reach the
summarized median and narrows the mean/median gap that inflates the
mean-based capital columns downstream. \code{move_slope}/\code{move_bias}/\code{disp_factor}
are in the units of the active space and default to the space-appropriate
shipped set; a non-default latent run can refit them via
\code{dev/suite/dynasty_latent_gate.R}. Value-free by construction: the transform
touches rank only, never the value curve.
}
\keyword{internal}
