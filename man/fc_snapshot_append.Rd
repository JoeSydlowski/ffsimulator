% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/12-fantasycalc.R
\name{fc_snapshot_append}
\alias{fc_snapshot_append}
\title{Append FantasyCalc value snapshots to a local database}
\usage{
fc_snapshot_append(
  db_path,
  configs = list(list(num_qbs = 1), list(num_qbs = 2))
)
}
\arguments{
\item{db_path}{path to the parquet database file}

\item{configs}{a list of argument-lists for \code{\link[=fc_dynasty_values]{fc_dynasty_values()}}; default
scrapes 1QB and superflex (12-team, 1PPR dynasty)}
}
\value{
(invisibly) \code{db_path}
}
\description{
(EXPERIMENTAL) Scrapes one or more league configurations and appends the
results to a growing parquet database keyed by \code{scraped_date} + settings, so
repeated runs build a value time-series. Re-running on the same day for the
same settings replaces those rows (idempotent).
}
