% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/15-deal-scores.R
\name{.ffs_packages}
\alias{.ffs_packages}
\title{Enumerate every package of the given sizes from a pool of assets}
\usage{
.ffs_packages(pool, sizes, must_include = NULL, gain_col = "value_to_me")
}
\arguments{
\item{pool}{a data.table with player_id, player_name, cur_value,
next_value_mean and the column named by \code{gain_col}}

\item{sizes}{package sizes to enumerate}

\item{must_include}{optional player_ids every package must contain}

\item{gain_col}{the additive win-value column to sum (\code{value_to_me} for the
side sending, \code{value_to_you} for the side receiving)}
}
\value{
a data.table: \code{n}, \code{ids} (list), \code{label}, \code{value}, \code{next_value},
\code{gain}, \code{top}, \code{has_pick}
}
\description{
Flattens \code{choose(nrow(pool), k)} combinations into one row per package with
its summed values pre-computed, so the caller can cross-join packages as plain
numeric vectors instead of looping.
}
\keyword{internal}
