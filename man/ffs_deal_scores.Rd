% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/15-deal-scores.R
\name{ffs_deal_scores}
\alias{ffs_deal_scores}
\title{Score trade deals for both sides}
\usage{
ffs_deal_scores(
  deals,
  dynasty,
  my_haircut = 0.8,
  playoff_value = 68,
  future_certainty = 0.905,
  future_reliability = NULL,
  pick_reliability = 0.55,
  format = "superflex",
  pos_lookup = NULL,
  fair_premium = 0.05,
  opp_edge_tol = 0.05,
  max_opp_drop = 0.15,
  opp_mode = c("market", "mirror"),
  champ_weight = 0,
  winwin_bonus = 0,
  traj_weight = 0
)
}
\arguments{
\item{deals}{a data.frame of deals. Required: \code{send_ids}, \code{recv_ids} (list
columns), \code{send_value}, \code{recv_value}, \code{future_capital_delta},
\code{my_playoff_delta}, \code{opp_playoff_delta}. Optional: \code{my_champ_delta},
\code{traj_bad}, \code{win_win}, \code{opp_playoff_before} (needed by \code{opp_mode = "mirror"}).}

\item{dynasty}{a dynasty outlook (player_id, cur_value, next_value_mean, pos)}

\item{my_haircut}{my win-now haircut, from \code{\link[=.ffs_haircut]{.ffs_haircut()}}}

\item{playoff_value}{dynasty value points per +1\% playoff odds (\code{k_P})}

\item{future_certainty}{fallback reliability for unknown positions}

\item{future_reliability}{named per-position reliability; \code{NULL} uses the
format default}

\item{pick_reliability}{level discount on draft-pick future value}

\item{format}{QB format, for the \code{future_reliability} default}

\item{pos_lookup}{optional named position vector keyed by player_id, when the
dynasty table lacks \code{pos}}

\item{fair_premium}{required overpay per extra piece (see \code{\link[=.ffs_market_edge]{.ffs_market_edge()}})}

\item{opp_edge_tol}{how far below the shape-fair price the opponent will still
deal (0.05 = I may take up to 5\% over fair)}

\item{max_opp_drop}{playoff-odds drop (0-1) that vetoes a deal outright}

\item{opp_mode}{"market" (default) or "mirror"}

\item{champ_weight, winwin_bonus, traj_weight}{my-side score weights}
}
\value{
\code{deals} with \code{adj_future_capital}, \code{prem}, \code{fair_gap}, \code{single_value},
\code{my_edge}, \code{opp_edge}, \code{opp_value_gain} (raw market value they net),
\code{opp_surplus} (value above the shape-fair price, in dynasty points - what
they judge on), \code{score}, \code{opp_score}, \code{gettable} and \code{grade}
}
\description{
My side keeps the full weighted model - playoff odds plus reliability-adjusted
future capital at my win-now haircut, in equivalent playoff percentage points.
}
\details{
The \strong{opponent side does not}. Real owners price a deal off current market
value, not off my per-position dynasty-reliability slopes, and a routine deal
that shaves a couple of points off their playoff odds is not something they
refuse. So \code{opp_mode = "market"} (the default) scores them on the market value
they net, and lets their playoff odds veto only a large drop - the case where
a trade genuinely guts them at a position. \code{opp_mode = "mirror"} restores the
previous behaviour (their playoff delta plus the negated mirror of my
reliability-adjusted future at their haircut) for A/B comparison.
}
