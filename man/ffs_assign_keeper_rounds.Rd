% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/14-keepers.R
\name{ffs_assign_keeper_rounds}
\alias{ffs_assign_keeper_rounds}
\title{Assign keepers to draft rounds}
\usage{
ffs_assign_keeper_rounds(
  costs,
  chosen_ids = NULL,
  max_keepers = Inf,
  max_top2 = Inf
)
}
\arguments{
\item{costs}{a dataframe of candidates - needs \code{player_id}, \code{base_round} and
(if \code{max_top2} is finite) \code{top2_prev}, as produced by \code{\link[=ffs_keeper_costs]{ffs_keeper_costs()}}}

\item{chosen_ids}{optional character vector of \code{player_id}s to assign;
defaults to every row of \code{costs}}

\item{max_keepers}{maximum number of keepers allowed}

\item{max_top2}{maximum number of keepers that may come from the previous
draft's first two rounds}
}
\value{
a list: \code{feasible} (flag), \code{reason} (character, \code{NA} when feasible),
\code{assignment} (a data.table of \code{player_id}, \code{base_round}, \code{keeper_round})
and \code{rounds_used} (the forfeited rounds)
}
\description{
(EXPERIMENTAL) A keeper costs the pick in its \code{base_round}, but a franchise
only owns one pick per round. When two keepers land on the same round the
usual house rule is that one of them \emph{bumps up} to the next earlier round
(two round-6 keepers cost a 6th and a 5th). Each player is therefore
assignable to any round in \verb{1..base_round}, each round is usable once, and
the franchise wants the assignment that forfeits the \emph{latest} (cheapest)
picks.
}
\details{
Processing players from the latest \code{base_round} backwards and giving each the
largest free round at or below its base is optimal here: a player with a
later base has strictly more options, so consuming his own round first never
blocks an earlier-base player who could not have used it anyway.
}
