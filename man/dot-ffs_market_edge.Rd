% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/15-deal-scores.R
\name{.ffs_market_edge}
\alias{.ffs_market_edge}
\title{Market edge on a trade, relative to the shape-fair price}
\usage{
.ffs_market_edge(send_value, recv_value, n_send, n_recv, fair_premium = 0.05)
}
\arguments{
\item{send_value, recv_value}{summed current market (dynasty) value each way}

\item{n_send, n_recv}{number of pieces each way}

\item{fair_premium}{required overpay per extra piece (default 0.05)}
}
\value{
a data.table with \code{prem}, \code{fair_gap}, \code{single_value}, \code{my_edge}, \code{opp_edge}
}
\description{
A multi-player package must \strong{overpay} the single better player it is traded
for: a premium player is worth more than a package of equal summed value, so
"fair" is not a zero value gap. The premium is measured against the \strong{single}
side (the one with fewer pieces) - \code{(package - single) / single} - which is the
convention the legacy \code{uneven_gap} band used, and the only one under which
"the package must be worth 5\% more" means what it says. Required premium is
\code{fair_premium} per extra piece.
}
\details{
\code{my_edge > 0} means I am winning the value trade by that fraction of the
single side's value; \code{opp_edge} is its negation, and is what the other owner
is really deciding on. \code{single_value} is returned so the edge can be converted
back into dynasty value points.
}
\keyword{internal}
