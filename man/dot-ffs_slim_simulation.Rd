% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/14-eval-engine.R
\name{.ffs_slim_simulation}
\alias{.ffs_slim_simulation}
\title{Strip a simulation down to what trade evaluation reads}
\usage{
.ffs_slim_simulation(base_simulation)
}
\arguments{
\item{base_simulation}{an \code{ff_simulation} from \code{return = "all"}}
}
\value{
a trimmed \code{ff_simulation}
}
\description{
A 2000-season \code{ff_simulation} is ~3 GB, and the evaluation path touches only
five of its components. \code{projected_scores} alone is ~950 MB and is never read;
\code{roster_scores} is 8.5M rows of which 9 columns matter; \code{optimal_scores}
carries two list-columns (\code{optimal_player_id}, \code{optimal_player_score}) that
nothing on this path uses. Dropping the rest takes the object to ~0.6 GB,
which is what makes it affordable for every parallel worker to hold a copy.
}
\details{
Only safe for the trade-evaluation path. In particular \code{\link[=ffs_pick_values]{ffs_pick_values()}}
needs \code{rosters}, which this drops.
}
\keyword{internal}
