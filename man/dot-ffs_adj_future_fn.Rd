% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/15-deal-scores.R
\name{.ffs_adj_future_fn}
\alias{.ffs_adj_future_fn}
\title{Build the reliability-adjusted future-capital function for a dynasty table}
\usage{
.ffs_adj_future_fn(
  dynasty,
  future_reliability = NULL,
  pick_reliability = 0.55,
  future_certainty = 0.905,
  format = "superflex",
  pos_lookup = NULL
)
}
\arguments{
\item{dynasty}{a dynasty outlook with player_id, cur_value, next_value_mean, pos}

\item{future_reliability}{named per-position vector; \code{NULL} uses \code{\link[=.ffs_future_reliability]{.ffs_future_reliability()}}}

\item{pick_reliability}{level discount on draft-pick value}

\item{future_certainty}{fallback reliability for unknown positions}

\item{format}{QB format, used only when \code{future_reliability} is \code{NULL}}
}
\value{
a function \verb{(send_ids, recv_ids, future_capital_delta)} returning the
reliability-adjusted future gain to the side doing the receiving
}
\description{
Reliability discounts the projected \strong{move} (\code{next - cur}), not the whole
level: a player's current market value is known, only next year's change is a
projection. Draft picks are pure speculation, so their future value is
level-discounted at \code{pick_reliability} - recovered as the residual of the raw
future delta that the real players do not explain.
}
\keyword{internal}
